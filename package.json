{
  "name": "audioreap-android",
  "version": "0.1.0",
  "private": true,
  "description": "Capacitor shell for the audioreap web UI. The APK is a thin native host; the app itself is served by the audioreap FastAPI service.",
  "scripts": {
    "android:sync": "cap sync android",
    "android:open": "cap open android",
    "android:icons": "python3 scripts/generate-android-icons.py",
    "android:build:debug": "npm run android:sync && cd android && ./gradlew assembleDebug",
    "android:build:release": "./scripts/build-apk.sh",
    "android:release": "./scripts/release-android.sh"
  },
  "devDependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/cli": "^8.5.0",
    "@capacitor/core": "^8.5.0"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "MIT"
}
